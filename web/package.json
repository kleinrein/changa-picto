{
    "scripts": {
        "start": "yarn run blendid",
        "build": "yarn run blendid -- build",
        "pages": "yarn run blendid -- gh-pages"
    },
    "dependencies": {
        "animejs": "^2.0.2",
        "blendid": "^4.0.1",
        "velocity-animate": "^1.5.0"
    }
}
